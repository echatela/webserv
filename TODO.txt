-> Parser config file (.cf subject, based on nginx):
-> Init serv
-> 

Focus structure projet ():



Class/Object list:
    .Config class
    .Listeners class
    .Server class (vect. listeners, + config class)
    .Client socket class
    .HTTP Request class
    .HTTP Response class
    .Reactor class (.cf https://en.wikipedia.org/wiki/Reactor_pattern#, https://en.wikipedia.org/wiki/C10k_problem)

Functions:

A LIRE !!!

https://en.wikipedia.org/wiki/Reactor_pattern#

https://en.wikipedia.org/wiki/C10k_problem (et tout les liens de la page !!!!! >:/)

- resumer la structure de config nginx (attention a la section serveur)
- parse parse
- Init serv
- construct non blocking loop (Reactor class ???????)
- parse request
- process request/define response
- send 
- 

Convention de nommage / Nomenclature

    . Class UpperCamelCase
        -> attributs: _camel_case
        -> methodes: camelCase


    . variable: gngng_gngngn
    . fonctions: camelCase -> dans des namespace


-> commentaires au dessus
-> tab de 8


_______________________________________________________________________
-> POURQUOI le server ne se lance pas a chaque fois ? // -> OK
-> Creer les locations == reflexion commune ? 
-> Reflechir au produit final
    -> architectures de fichiers / front
    -> schema d actions ex(je clic sur un tel bouton, ca ouvre tel page)
-> Clean reponses GET
-> Construction reponses DELETE/POST
